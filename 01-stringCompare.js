var string1 = window.prompt("Enter a string")
var string2 = window.prompt("Enter another string")

if (string1 == string2){
  alert("the strings are the same!")
}
else{
  alert("the strings are not the same!")
}
